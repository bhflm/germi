import { NextPage } from 'next';
import { TwitterTimeline } from '../components/Twitter';

const Home: NextPage<{ props: any }> = props => (
  // <TwitterTimeline/> not gonna use this atm
  // header
  // main layout
  // footer
  <div>
    QUEDENSE EN SUS CASAS Y LAVENSE LAS MANOS
  </div>
);

export default Home;